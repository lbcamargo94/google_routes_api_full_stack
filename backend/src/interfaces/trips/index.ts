interface ITrips {
  id: string;
  destination: string;
  distance: number;
  driver_id: string;
  duration: string;
  origin: string;
  price: number;
  customer_id: string;
}

export type { ITrips };
